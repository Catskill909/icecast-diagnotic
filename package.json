{
  "name": "icecast-monitor",
  "version": "1.0.0",
  "description": "KPFT Icecast stream diagnostics with listener-impact reporting and email alerts",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/",
    "setup": "node scripts/setup.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "express": "^4.21.0",
    "nodemailer": "^9.0.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
