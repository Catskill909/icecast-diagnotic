node_modules/
.env
.env.*
!.env.example
data/
*.log
.DS_Store
incidents-backup.json

# Geo databases. Licensed files supplied by the deployer, tens of megabytes,
# and MaxMind's EULA restricts redistributing theirs — so a database that lands
# in the working tree must never be committed by accident.
*.mmdb
*.mmdb.gz

# Private planning/discussion notes — never committed
WORKING-NOTES.md
